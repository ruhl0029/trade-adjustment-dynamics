
# Empirics: Readme
These files create table 1. 

* In the Colombia folder, the file `Colombia.zip` contains the data. Unzip and then run the .do file.
* In the Chile folder, the .do file performs the calculations needed. 
* In the Compustat folder, there are no data files. Compustat requires a subscription. The files in the `Do Files` directory set up the data set. The files in the `Entryexit_Compustat` directory perform the calculations needed.

